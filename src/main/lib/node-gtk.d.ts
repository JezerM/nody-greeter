/**
 * TODO: remove once @types/node-gtk exists
 *
 * These type definitions for `node-gtk` do not include the individual
 * type-definitions for most libraries. Instead you must include the specific
 * type definitions for the gobject library you wish to use.
 */

declare module "node-gtk" {
  /**
   * These type definitions for `node-gtk` do not include the individual
   * type-definitions for most libraries. Instead you must include the specific
   * type definitions for the gobject library you wish to use.
   */
  export function require(name: never, version?: never): never;
}
